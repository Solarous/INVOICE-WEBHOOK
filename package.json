{
  "name": "paypal-discord-bridge",
  "version": "1.0.0",
  "description": "Forwards PayPal webhook events (payments & invoices) to a Discord webhook",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
